import { createMuiTheme } from '@material-ui/core/styles';
const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(0, 188, 235)',
    },
    secondary: {
      main: 'rgb(110, 190, 74)',
    }
  },
});
export default theme;